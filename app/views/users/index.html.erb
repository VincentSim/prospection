<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Name</th>
      <th>Email</th>
      <th>Client</th>
      <th>Contacted</th>
      <th>Contacted one</th>
      <th>Contacted two</th>
      <th>Contacted three</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.first_name %></td>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.client %></td>
        <td><%= user.contacted %></td>
        <td><%= user.contacted_one %></td>
        <td><%= user.contacted_two %></td>
        <td><%= user.contacted_three %></td>
        <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
        <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path %>
